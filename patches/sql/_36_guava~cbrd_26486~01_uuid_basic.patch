--- cases/01_uuid_basic.sql
+++ cases/01_uuid_basic.sql
@@ -138,3 +138,7 @@
        substr(uuid_format(uuid(7.49)), 15, 1) v_7_49;
 -- negative decimal near zero rounds to 0 (valid, UUIDv4), not treated as -1.
 select substr(uuid_format(uuid(-0.49)), 15, 1) v_neg_0_49;
+
+-- Session variables live on the connection, and CUBRID holds twenty of
+-- them. A case that leaves one spends the next case's budget.
+DROP VARIABLE @u7_first, @u7_second;
--- answers/01_uuid_basic.answer
+++ answers/01_uuid_basic.answer
@@ -210,3 +210,5 @@
 v_neg_0_49    
 4     
 
+===================================================
+0
